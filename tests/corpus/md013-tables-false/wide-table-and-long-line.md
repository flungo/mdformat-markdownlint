# A wide table

| A wide table, wider than eighty columns in its source, which the rule does not measure | b |
| -- | -- |
| a | b |

Ninety columns of words, with whitespace past the eightieth column, on one line x x x x xx
