# Code spans

A span with a leading space, ` code`, and one with a trailing space, `code `.

A span with one space each side, ` code `, which CommonMark strips.

A span with two spaces each side, `  code  `.
