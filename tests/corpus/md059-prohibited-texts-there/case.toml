# MD059 descriptive-link-text with `prohibited_texts` naming `there` alone:
# only the link whose text is `there` is a finding, one on the pooled
# document where the default reports two.
status = "neutral"
rule = "MD059"

[inputs."link-texts.md"]
from = "link-texts.md"
findings = 1
rewritten = true
