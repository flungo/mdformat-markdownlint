# MD027 no-multiple-space-blockquote with `list_items` false: two spaces
# before a list item in a blockquote are not a finding, where the default
# reports the pooled document's two, and mdformat alone writes one either
# way; the other constructs report as at the default.
status = "guaranteed"
rule = "MD027"

[inputs."blockquote-list-with-two-spaces.md"]
from = "blockquote-list-with-two-spaces.md"
findings = 0
incidental = ["MD007"]
rewritten = true

[inputs."blockquote-with-two-spaces.md"]
from = "blockquote-with-two-spaces.md"
findings = 6
rewritten = true
