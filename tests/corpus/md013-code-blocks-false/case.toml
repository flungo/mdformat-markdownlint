# MD013 line-length with `code_blocks` false: the pooled document's code line
# is not measured, three findings where the default reports four.
status = "neutral"
rule = "MD013"

[inputs."long-lines.md"]
from = "long-lines.md"
findings = 3
rewritten = true
